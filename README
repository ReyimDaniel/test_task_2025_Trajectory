Для запуска приложения используйте файлы в 'src/controllers':
1. 'main_all_func' для запуска всех функций разом (в файле 'src/config' можно изменить время и дату для функций).
2. 'main_console' скрипт запуска консольного приложения в котором можно произвольно ввести дату и выбрать необходимую функцию.

Для запуска unit-тестов используйте файл 'test_work_schedule' из 'src/tests':

В 'src/models' находятся файлы с классами и функциями.
